model = "gpt-5.3-codex"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Scope:
- Do read-only discovery only.
- Prefer rg for search.
- Summarize affected files with concrete paths.

Repository notes:
- Shared NixOS modules: hosts/nixos/common/
- Host-specific modules: hosts/nixos/<host>/
- Home Manager modules: home-manager/
- Custom packages: pkgs/<name>/default.nix
"""
