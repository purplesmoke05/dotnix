diff --git a/src/core/toml_filter.rs b/src/core/toml_filter.rs
index 977e8d9..80e1b2f 100644
--- a/src/core/toml_filter.rs
+++ b/src/core/toml_filter.rs
@@ -887,6 +887,19 @@ mod tests {
             .expect("expected at least one filter")
     }
 
+    #[test]
+    fn test_production_registry_loads_builtin_filters() {
+        let registry = TomlFilterRegistry::load();
+
+        assert!(
+            registry
+                .filters
+                .iter()
+                .any(|filter| filter.name == "shellcheck"),
+            "production registry must load embedded built-in filters"
+        );
+    }
+
     #[test]
     fn command_matches_filter_agrees_with_find_matching_filter() {
